# ES Modules

ES Modules (ESM) é o padrão oficial do [[Javascript]] para dividir e reutilizar código entre diferentes arquivos usando as palavras-chave `import` e `export`.

Eles funcionam de forma nativa em navegadores modernos e no [[Node.js]], substituindo o antigo sistema CommonJS (`require`).